define(['user', 'users', 'userrow', 'allusers'], function (UserModel, UsersCollection, UserRowView, AllUsersView) {
    return {
        start: function () {
            alert("Ok! It's me!");
        }
    };
});
